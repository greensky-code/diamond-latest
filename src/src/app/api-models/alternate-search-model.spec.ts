import { AlternateSearchModel } from './alternate-search-model';

describe('AlternateSearchModel', () => {
  it('should create an instance', () => {
    expect(new AlternateSearchModel()).toBeTruthy();
  });
});
