import { ProviderMasterView } from './provider-master-view';

describe('ProviderMasterView', () => {
  it('should create an instance', () => {
    expect(new ProviderMasterView()).toBeTruthy();
  });
});
