import { LineOfBusinessMaster.Model } from './line-of-business-master.model';

describe('LineOfBusinessMaster.Model', () => {
  it('should create an instance', () => {
    expect(new LineOfBusinessMaster.Model()).toBeTruthy();
  });
});
