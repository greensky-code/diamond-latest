import { ClaimBenefitAccum.Model } from './claim-benefit-accum.model';

describe('ClaimBenefitAccum.Model', () => {
  it('should create an instance', () => {
    expect(new ClaimBenefitAccum.Model()).toBeTruthy();
  });
});
