import { CiebExchangeRate } from './cieb-exchange-rate.model';

describe('CiebExchangeRate', () => {
  it('should create an instance', () => {
    expect(new CiebExchangeRate()).toBeTruthy();
  });
});
