import { BenefitRuleStatus } from './benefit-rule-status';

describe('BenefitRuleStatus', () => {
  it('should create an instance', () => {
    expect(new BenefitRuleStatus()).toBeTruthy();
  });
});
