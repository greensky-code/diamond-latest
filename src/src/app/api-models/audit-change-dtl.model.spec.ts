import { AuditChangeDtl.Model } from './audit-change-dtl.model';

describe('AuditChangeDtl.Model', () => {
  it('should create an instance', () => {
    expect(new AuditChangeDtl.Model()).toBeTruthy();
  });
});
