import { UserDefinedValidateCode } from './user-defined-validate-code';

describe('UserDefinedValidateCode', () => {
  it('should create an instance', () => {
    expect(new UserDefinedValidateCode()).toBeTruthy();
  });
});
