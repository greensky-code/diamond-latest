import { ConProvGroupDetailPrimaryKey } from './con-prov-group-detail-primary-key';

describe('ConProvGroupDetailPrimaryKey', () => {
  it('should create an instance', () => {
    expect(new ConProvGroupDetailPrimaryKey()).toBeTruthy();
  });
});
