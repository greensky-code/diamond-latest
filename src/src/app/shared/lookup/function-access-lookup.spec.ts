import { FunctionAccessLookup } from './function-access-lookup';

describe('FunctionAccessLookup', () => {
  it('should create an instance', () => {
    expect(new FunctionAccessLookup()).toBeTruthy();
  });
});
