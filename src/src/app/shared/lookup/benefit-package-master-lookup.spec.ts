import { BenefitPackageMasterLookup } from './benefit-package-master-lookup';

describe('BenefitPackageMasterLookup', () => {
  it('should create an instance', () => {
    expect(new BenefitPackageMasterLookup()).toBeTruthy();
  });
});
