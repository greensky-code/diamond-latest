import { GlReferenceLookup } from './gl-reference-lookup';

describe('GlReferenceLookup', () => {
  it('should create an instance', () => {
    expect(new GlReferenceLookup()).toBeTruthy();
  });
});
